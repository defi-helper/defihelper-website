import { style } from '@vanilla-extract/css';

export const root = style({
  display: 'flex',
  alignItems: 'center',
  borderRadius: 16,
  padding: 16
});

export const icon = style({
  marginRight: 8,
  width: 40,
  height: 40
});
